DEFAULT_USER=mmcmullen

# Load modules
{
    module load clang-16.0
    module load git-latest
    module load git-lfs-latest
    module load golang-lateset
    module load neovim-latest
} &> /dev/null

PATH+=":${NEOVIM_PREFIX}/bin"
