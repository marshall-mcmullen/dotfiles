DEFAULT_USER=mmcmullen
unset SSH_CLIENT

# Load modules
{
    module load btop-1.3.2
    module load clang-20.1.0
    module load git-latest
    module load git-lfs-latest
    module load golang-latest
    module load jtnctl-latest
    module load neovim-latest
    module load nodejs-latest
} &> /dev/null

PATH+=":${NEOVIM_PREFIX}/bin"

# Aliases
alias jsh='jtnctl --shell --host'
alias jn='jtnctl --host'
